#include <iostream>
#include <cmath>
#include <map>
#include <stack>
using namespace std;

int main(){
    ios_base::sync_with_stdio(0);                         
    cin.tie(0);
    cout.tie(0);
    stack<map<string,int64_t>> memory;
    stack<string> local_vars;// -> deleting after left block { }


    return 0;
}